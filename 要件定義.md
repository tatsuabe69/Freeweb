# 要件定義書
## ブラウザ完結型ファイル処理ツール

---

## 1. 非機能要件（絶対に変えてはいけないもの）

### 1.1 プライバシー原則

```
MUST: ユーザーのファイルはサーバーに一切送信されない
MUST: すべての処理はユーザーのブラウザ内で完結する
MUST: ファイルの内容をログに記録しない
MUST: ユーザーのファイルをキャッシュ・保存しない
```

### 1.2 アクセス原則

```
MUST: ユーザー登録不要
MUST: ログイン不要
MUST: メールアドレス収集なし
MUST: クレジットカード情報収集なし
```

### 1.3 UX原則

```
MUST: 広告を一切表示しない
MUST: 処理結果に透かしを入れない
MUST: ファイルサイズ制限を設けない（ブラウザメモリ依存のみ）
MUST: 使用回数制限を設けない
MUST: ページロードから使用開始まで3秒以内
MUST: 処理完了までの進捗を視覚的に表示する
```

---

## 2. 技術スタック

### 2.1 フロントエンド

| 用途 | 採用技術 | 理由 |
|---|---|---|
| フレームワーク | Next.js（Static Export）または Astro | 静的出力でCloudflare Pages対応 |
| スタイリング | Tailwind CSS | 高速開発・一貫性 |
| UIコンポーネント | shadcn/ui | アクセシビリティ・品質 |

### 2.2 ファイル処理ライブラリ

| 機能 | ライブラリ | 動作場所 |
|---|---|---|
| PDF読み取り・表示 | PDF.js（Mozilla） | ブラウザ |
| PDF生成・編集 | pdf-lib | ブラウザ |
| 動画処理全般 | ffmpeg.wasm | ブラウザ |
| 画像圧縮・変換 | Canvas API + browser-image-compression | ブラウザ |
| OCR（文字認識） | Tesseract.js | ブラウザ |
| Excel/CSV処理 | SheetJS（xlsx） | ブラウザ |

### 2.3 インフラ

| 用途 | サービス | コスト |
|---|---|---|
| ホスティング | Cloudflare Pages | $0（帯域無制限） |
| API（必要時） | Cloudflare Workers | $0（10万req/日） |
| ドメイン | Cloudflare Registrar | 約$10/年 |
| 分析 | Cloudflare Web Analytics | $0 |

---

## 3. 機能要件

### 3.1 フェーズ1：PDFツール群

#### PDF結合（Merge）
- 複数PDFをドラッグ＆ドロップで追加
- ページ順序をドラッグで並び替え可能
- 結合後のプレビュー表示
- 処理：`pdf-lib` でクライアントサイド処理
- 出力：単一PDFファイルとしてダウンロード

#### PDF分割（Split）
- ページ指定での分割（例: 1-3ページ、4-6ページ）
- 1ページずつの分割
- ページ範囲のプレビュー
- 出力：ZIPまたは個別PDFダウンロード

#### PDF圧縮（Compress）
- 圧縮レベル選択（低・中・高）
- 圧縮前後のファイルサイズ比較表示
- 画像品質のトレードオフを視覚的に説明
- 出力：圧縮済みPDF

#### PDF回転（Rotate）
- ページ単位での回転（90°、180°、270°）
- 全ページ一括回転
- プレビュー付き

#### PDF → 画像変換
- 出力形式：JPG、PNG、WebP
- 解像度選択（72dpi、150dpi、300dpi）
- 一括変換 → ZIPダウンロード

#### 画像 → PDF変換
- 複数画像をPDFに結合
- ページサイズ設定（A4、Letter、画像サイズに合わせる）

#### PDFページ並び替え
- サムネイル表示でのドラッグ&ドロップ操作
- ページ削除機能

---

### 3.2 フェーズ2：動画ツール群

#### 動画圧縮（最重要機能）
- プリセット選択
  - Discord送信用（8MB以下）
  - LINE送信用（50MB以下）
  - Twitter投稿用（512MB以下）
  - カスタム（ファイルサイズ指定）
- 圧縮前後のサイズ・品質プレビュー
- 処理：`ffmpeg.wasm`（完全ブラウザ内）
- 対応形式：MP4、MOV、AVI、WebM

#### 動画 → GIF変換
- 開始・終了時間の指定
- フレームレート・サイズ調整
- プレビュー表示

#### 動画トリミング
- タイムライン上でのドラッグ操作
- フレーム単位の精密指定

#### SNS向けアスペクト比変換
- プリセット：TikTok/Reels（9:16）、Instagram（1:1）、YouTube（16:9）
- クロップ位置の調整（どの部分を切り出すか）

#### 音声抽出
- 動画からMP3/WAV/AACを抽出
- ビットレート選択

---

### 3.3 フェーズ3：拡張ツール群

#### HEIC → 各形式変換
- 出力形式：JPG、PNG、WebP
- 品質設定
- 一括変換対応

#### 画像圧縮・変換
- PNG/JPG/WebP相互変換
- 一括処理
- サイズリサイズ

#### PDF差分比較
- 2つのPDFのテキスト差分を色分け表示
- 追加（緑）・削除（赤）・変更（黄）

#### 日本語OCR
- スキャンPDF・画像からテキスト抽出
- 日本語・英語混在対応
- テキストのコピー・TXTダウンロード

#### 開発者向け変換ツール
- JSON ↔ TypeScript型定義
- JSON ↔ YAML
- SVG → JSX
- Base64 エンコード/デコード
- CSS → Tailwind CSS

---

## 4. UI/UX要件

### 4.1 レイアウト原則

```
- トップページ: ツール一覧をカード形式で表示
- 各ツールページ: シングルページ、上部にファイルドロップ領域
- ドロップゾーン: 画面の大部分を占める（迷わない）
- 処理中: プログレスバー + パーセンテージ表示
- 完了後: ダウンロードボタンが目立つ位置に
```

### 4.2 ファイル入力

```
- ドラッグ＆ドロップ対応（全ツール共通）
- クリックしてファイル選択 も対応
- 複数ファイル同時投入対応（結合系ツール）
- ブラウザ内でのファイルサイズ警告（メモリ超過リスク時）
```

### 4.3 アクセシビリティ

```
- キーボード操作完全対応
- スクリーンリーダー対応（ARIAラベル）
- ダークモード対応
- モバイル（スマートフォン）完全対応
- 日本語・英語UI切替
```

### 4.4 パフォーマンス目標

```
- First Contentful Paint: < 1.5秒
- Largest Contentful Paint: < 2.5秒（Core Web Vitals合格）
- ffmpeg.wasm初回ロード: プログレッシブローディングで体験を損なわない
- PDF処理（10MB以下）: < 5秒
- 動画圧縮: 進捗バー表示（処理時間は許容）
```

---

## 5. プライバシー・セキュリティ要件

### 5.1 技術的保証

```
- Content Security Policy (CSP) 設定: 外部への意図しない送信を防止
- HTTPS強制
- サードパーティスクリプト使用禁止（Google Analytics等のサーバー送信型を除外）
- 分析: Cloudflare Web Analytics（プライバシーファースト）を使用
```

### 5.2 ヘッダー設定（ffmpeg.wasm使用に必要）

```
Cross-Origin-Embedder-Policy: require-corp
Cross-Origin-Opener-Policy: same-origin
```

### 5.3 プライバシーポリシー表示

```
- フッターにシンプルなプライバシーポリシーページ
- 「収集するデータ: なし」を明記
- 「ファイルはサーバーに送信されません」をトップページに常時表示
```

---

## 6. SEO要件

### 6.1 ページ構成

```
/                    → トップ（全ツール一覧）
/pdf/merge           → PDF結合
/pdf/compress        → PDF圧縮
/pdf/split           → PDF分割
/pdf/to-image        → PDF→画像
/video/compress      → 動画圧縮
/video/to-gif        → 動画→GIF
/video/trim          → 動画トリミング
/image/compress      → 画像圧縮
/image/heic-to-jpg   → HEIC変換
...（各ツールに専用URL）
```

### 6.2 各ページのSEO要素

```
- title: "[ツール名] - 無料・広告なし・登録不要 | サービス名"
- description: 機能説明 + 「ファイルはサーバーに送られません」
- OGP画像: ツールのスクリーンショット
- 構造化データ: WebApplication スキーマ
- 日本語・英語両対応（hreflang設定）
```

---

## 7. ブランド要件（未確定）

### 7.1 命名候補

| 案 | 印象 |
|---|---|
| LocalTools | 機能的・わかりやすい |
| BrowserBox | ボックス感・完結感 |
| ZeroUpload | 差別化が一言でわかる |
| Filecraft | 職人感・クリエイティブ |
| PixelVault | セキュリティ・プライバシー感 |

### 7.2 キャッチコピー候補

```
"Your files never leave your browser."
「ファイルは、あなたのものです。」
"Free. Private. No limits."
「アップロード不要。広告なし。ずっと無料。」
```

---

## 8. 開発ロードマップ

### Week 1-2: フェーズ1 MVP
- [ ] Next.js + Tailwind セットアップ
- [ ] Cloudflare Pages デプロイ設定
- [ ] PDF結合・分割・圧縮・回転 実装
- [ ] ドラッグ＆ドロップUI実装
- [ ] プライバシー表示・基本ページ構成

### Week 3-4: フェーズ2
- [ ] ffmpeg.wasm 統合
- [ ] 動画圧縮（Discord/LINE/SNSプリセット）
- [ ] 動画→GIF変換
- [ ] SNS向けアスペクト比変換

### Week 5-6: 品質・SEO
- [ ] Core Web Vitals最適化
- [ ] 各ツール専用ページのSEO設定
- [ ] モバイル最適化
- [ ] ダークモード実装
- [ ] 日本語・英語UI

### Week 7: ローンチ
- [ ] Hacker News "Show HN" 投稿準備
- [ ] Reddit / Twitter 投稿素材作成
- [ ] Product Hunt ローンチ準備
- [ ] ドメイン取得・設定

---

## 9. 制約・リスク

| リスク | 内容 | 対策 |
|---|---|---|
| ffmpeg.wasm 初回ロード | 約25MBのダウンロード | 遅延ロード・プログレスバー表示 |
| モバイルのメモリ制限 | 大ファイル処理でクラッシュの可能性 | ファイルサイズ警告・推奨サイズ表示 |
| 日本語フォント埋め込み | PDFに日本語を挿入する場合フォントが重い | フォントサブセット化 |
| SharedArrayBuffer要件 | ffmpeg.wasmに特定HTTPヘッダーが必要 | Cloudflare Pagesで設定（_headersファイル） |
| ブラウザ互換性 | 古いブラウザでWASMが動かない | モダンブラウザ推奨表示 |

---

*作成日: 2026年2月22日*
*バージョン: 1.0*
